This is the deep learning library written in C from scratch using in characted recognition.
The library uses the Convoluted Neural Network to recognise characters after it was trained with semeion data.
